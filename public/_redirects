# API routes should be handled by the function first
/api/* /api/:splat 200

# Static assets should be served directly
/assets/* /assets/:splat 200
/favicon.ico /favicon.ico 200
/favicon.svg /favicon.svg 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/manifest.json /manifest.json 200

# Logo assets
/logo-*.svg /logo-:splat.svg 200

# Build assets
/build/* /build/:splat 200

# All other routes should go through the Remix handler
/* /index.html 200